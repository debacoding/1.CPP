#include<bits/stdc++.h>
using namespace std;

int main(){
	int n;
	cin >> n; // array size

  int arr[n];
	for(int i = 0; i < n; i++ ){
		cin << arr[n];
	}

	reverse(arr, arr + n );
 
    for(int i = 0; i < n; i++ ){
		   cout << arr[i] << " ";
	   }
	return 0;
}
